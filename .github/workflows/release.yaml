# .github/workflows/release.yaml, publish to crates.io + GitHub Release on tag push.
#
#   * verify-version, the git tag is the single source of truth; refuse to
#     publish if Cargo.toml's version doesn't match the tag.
#   * crates-io     , publish via OIDC Trusted Publishing (no stored token).
#   * sbom          , SPDX + CycloneDX, attached to the release.
#   * github-release, notes + SBOMs.
#
# PREREQUISITE for the crates-io job: own `sua` on crates.io and configure
# Trusted Publishing (OIDC) on the crate, pointing at this repo + this workflow.
# The first publish of a brand-new crate needs a one-shot `publish-new` token
# (Trusted Publishing can't bootstrap a crate that doesn't exist yet).
#
# The PyPI wheel jobs (build-wheels*, build-sdist, pypi-publish) are GATED behind
# the repo variable `PYPI_PUBLISH` (Settings → Variables → Actions). They stay
# dormant, and crates.io releases keep working, until a pending Trusted
# Publisher for `sua` is configured on PyPI (repo + workflow + `pypi`
# environment) and `PYPI_PUBLISH` is set to `true`.
name: Release

on:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read

jobs:
  verify-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cargo version must equal the tag
        run: |
          tag_version="${GITHUB_REF_NAME#v}"
          cargo_version="$(grep -m1 '^version = ' Cargo.toml | sed -E 's/^version = "(.*)"/\1/')"
          echo "tag=$tag_version cargo=$cargo_version"
          if [ "$tag_version" != "$cargo_version" ]; then
            echo "::error::Cargo.toml version ($cargo_version) does not match tag ($tag_version), bump version in the release commit."
            exit 1
          fi

  crates-io:
    needs: verify-version
    runs-on: ubuntu-latest
    permissions:
      id-token: write # mint a short-lived crates.io token via GitHub OIDC, no stored secret
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      # Idempotent: the brand-new crate's first version is bootstrapped with a
      # one-shot manual `cargo publish` (Trusted Publishing can't create a crate
      # that doesn't exist yet). If this tag's version is already on crates.io,
      # skip, so re-running a tag, or tagging the bootstrap release, stays green.
      - name: Already on crates.io?
        id: check
        # crates.io returns 403 to requests without a descriptive User-Agent, so
        # the -A header is mandatory (a bare curl would 403 and look "unpublished").
        run: |
          ver="${GITHUB_REF_NAME#v}"
          ua="sua-release-ci (github.com/Real-Time-Telecom-B-V/sua)"
          if curl -sf -A "$ua" "https://crates.io/api/v1/crates/sua/${ver}" -o /dev/null; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "::notice::sua ${ver} already on crates.io, skipping publish."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Authenticate to crates.io (OIDC Trusted Publishing)
        if: steps.check.outputs.exists == 'false'
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
        id: auth
      - name: Publish
        if: steps.check.outputs.exists == 'false'
        run: cargo publish --locked
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  sbom:
    needs: verify-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Install cargo-sbom
        run: cargo install cargo-sbom
      - name: Generate SPDX + CycloneDX SBOMs
        run: |
          cargo sbom --output-format spdx_json_2_3 > "sua-${GITHUB_REF_NAME}.spdx.json"
          cargo sbom --output-format cyclone_dx_json_1_4 > "sua-${GITHUB_REF_NAME}.cdx.json"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sbom
          path: |
            sua-*.spdx.json
            sua-*.cdx.json

  # ── PyPI wheels (gated on the PYPI_PUBLISH repo variable; see header) ────────
  build-wheels:
    name: wheels ${{ matrix.platform.runner }} ${{ matrix.platform.target }}
    if: vars.PYPI_PUBLISH == 'true'
    needs: verify-version
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - { runner: ubuntu-latest, target: x86_64 }
          - { runner: ubuntu-latest, target: aarch64 }
          - { runner: macos-13, target: x86_64 }
          - { runner: macos-14, target: aarch64 }
          - { runner: windows-latest, target: x64 }
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.x"
      # abi3-py39 (set in Cargo.toml) → one forward-compatible wheel per platform
      # covering regular CPython 3.9+.
      - uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          manylinux: auto
          sccache: "true"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-${{ matrix.platform.runner }}-${{ matrix.platform.target }}
          path: dist

  build-wheels-freethreaded:
    name: wheels-ft ${{ matrix.py }} ${{ matrix.platform.runner }}
    if: vars.PYPI_PUBLISH == 'true'
    needs: verify-version
    # A free-threaded interpreter cannot load an abi3 wheel, and abi3t is 3.15+
    # only, so today's free-threaded builds get version-specific (cp3XXt) wheels.
    # Best-effort until the toolchain settles; does not block the abi3 wheels.
    continue-on-error: true
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        py: ["3.13t", "3.14t"]
        platform:
          - { runner: ubuntu-latest, target: x86_64 }
          - { runner: macos-14, target: aarch64 }
          - { runner: windows-latest, target: x64 }
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.py }}
      - uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --interpreter python${{ matrix.py }}
          manylinux: auto
          sccache: "true"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wheels-ft-${{ matrix.py }}-${{ matrix.platform.runner }}-${{ matrix.platform.target }}
          path: dist

  build-sdist:
    if: vars.PYPI_PUBLISH == 'true'
    needs: verify-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          command: sdist
          args: --out dist
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sdist
          path: dist

  pypi-publish:
    name: Publish to PyPI (Trusted Publishing)
    if: vars.PYPI_PUBLISH == 'true'
    needs: [build-wheels, build-wheels-freethreaded, build-sdist]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # mint the PyPI publish token via OIDC, no stored secret
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          pattern: "wheels-*"
          merge-multiple: true
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: dist
          pattern: "sdist"
          merge-multiple: true
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
        with:
          packages-dir: dist

  github-release:
    needs: [crates-io, sbom]
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the release and upload assets
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Download SBOMs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: artifacts
          merge-multiple: true
      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          generate_release_notes: true
          files: |
            artifacts/*.spdx.json
            artifacts/*.cdx.json
